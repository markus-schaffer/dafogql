% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mat_jordstykke2.R
\name{mat_jordstykke2}
\alias{mat_jordstykke2}
\title{Query MAT Jordstykke via GraphQL}
\usage{
mat_jordstykke2(
  client_id,
  client_secret,
  time,
  sfe,
  nodes = c("id_lokalId", "samletFastEjendomLokalId"),
  node_names = c(id_lokalId = "plot_id", samletFastEjendomLokalId = "sfe"),
  batch_size = 100L
)
}
\arguments{
\item{client_id}{OAuth 2.0 client ID}

\item{client_secret}{OAuth 2.0 client secret}

\item{time}{DateTime string in ISO 8601 format (e.g. "2026-03-16T00:00:00Z")}

\item{sfe}{Character vector of one or more SFEs (samlet Fast Ejendom)}

\item{nodes}{Character vector of node field names to return from the query.
Defaults to
\code{c("id_lokalId", "samletFastEjendomLokalId")}.
Must be valid field names for the MAT_Jordstykke type.}

\item{node_names}{Optional named character vector mapping API field names to output
column names, e.g. \code{c(id_lokalId = "id")}. Fields not present
in the mapping are returned with their original API name. Defaults
to \code{c(id_lokalId = "plot_id", samletFastEjendomLokalId =
                     "sfe")}. Pass \code{NULL} to skip renaming entirely.}

\item{batch_size}{Maximum number of IDs per request (default: 100). More than 100 will be rejected by the API.}
}
\value{
A data frame with columns as specified by \code{nodes}, renamed according to \code{node_names} where a mapping exists
}
\description{
Query MAT Jordstykke via GraphQL
}
\examples{
\dontrun{
result <- mat_jordstykke2(
  client_id     = Sys.getenv("CLIENT_ID"),
  client_secret = Sys.getenv("CLIENT_SECRET"),
  time          = "2026-03-16T00:00:00Z",
  plot_id       = "0a3f50ca-201a-32b8-e044-0003ba298018"
)
}
}
