% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dar_adressepunkt.R
\name{dar_adressepunkt}
\alias{dar_adressepunkt}
\title{Query DAR Adressepunkt via GraphQL}
\usage{
dar_adressepunkt(
  client_id,
  client_secret,
  time,
  bldg_adr_id,
  nodes = c("id_lokalId", "position {wkt crs}"),
  node_names = c(id_lokalId = "bldg_adr_id", position.wkt = "position.wkt", position.crs
    = "position.crs"),
  batch_size = 100L
)
}
\arguments{
\item{client_id}{OAuth 2.0 client ID}

\item{client_secret}{OAuth 2.0 client secret}

\item{time}{DateTime string in ISO 8601 format (e.g. "2026-03-16T00:00:00Z")}

\item{bldg_adr_id}{Character vector of one or more building address IDs (DAR Husnummer)}

\item{nodes}{Character vector of node field names to return from the query.
Defaults to
\code{c("id_lokalId", "position { wkt crs }")}.
Must be valid field names for the DAR_Adressepunkt type.}

\item{node_names}{Optional named character vector mapping API field names to output
column names, e.g. \code{c(id_lokalId = "id")}. Fields not present
in the mapping are returned with their original API name. Defaults
to \code{c(id_lokalId = "bldg_adr_id", position.wkt = "position.wkt",
                     position.crs = "position.crs")}. Pass \code{NULL} to skip
renaming entirely.}

\item{batch_size}{Maximum number of IDs per request (default: 100). More than 100 will be rejected by the API.}
}
\value{
A data frame with columns as specified by \code{nodes}, renamed according to \code{node_names} where a mapping exists
}
\description{
Query DAR Adressepunkt via GraphQL
}
\examples{
\dontrun{
result <- dar_adressepunkt(
  client_id     = Sys.getenv("CLIENT_ID"),
  client_secret = Sys.getenv("CLIENT_SECRET"),
  time          = "2026-03-16T00:00:00Z",
  bldg_adr_id   = "0a3f507a-e179-32b8-e044-0003ba298018"
)
}
}
