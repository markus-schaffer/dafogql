% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_graphql_query.R
\name{batch_graphql_query}
\alias{batch_graphql_query}
\title{Execute a GraphQL query in batches and combine the results}
\usage{
batch_graphql_query(ids, batch_size, fetch_fn)
}
\arguments{
\item{ids}{Character vector of IDs to query}

\item{batch_size}{Maximum number of IDs per request}

\item{fetch_fn}{A function that accepts a character vector of IDs and an
optional cursor string, and returns a list with elements
\verb{$pageInfo} (containing \verb{$hasNextPage} and \verb{$endCursor})
and \verb{$nodes}}
}
\value{
A list with:
\item{page_info}{pageInfo from the last page of the last batch}
\item{nodes}{Row-bound data frame of all nodes across all batches and pages}
}
\description{
Splits \code{ids} into chunks of at most \code{batch_size}, calls \code{fetch_fn} for each
chunk (looping through all pages via cursor-based pagination), then
row-binds the \code{nodes} data frames and returns the combined result.
}
\keyword{internal}
