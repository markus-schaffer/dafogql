% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/daf_query.R
\name{daf_query}
\alias{daf_query}
\title{Shared internal GraphQL query engine for Datafordeler APIs}
\usage{
daf_query(
  client_id,
  client_secret,
  time,
  ids,
  api_path,
  query_var_type,
  filter_type,
  gql_var,
  resource_name,
  where_builder,
  nodes,
  node_names,
  batch_size,
  id_prep = function(x) {
     x <- unique(x)
     x[!is.na(x)]
 }
)
}
\arguments{
\item{client_id}{OAuth 2.0 client ID}

\item{client_secret}{OAuth 2.0 client secret}

\item{time}{DateTime string in ISO 8601 format
(e.g. \code{"2026-03-16T00:00:00Z"})}

\item{ids}{Vector of IDs to query}

\item{api_path}{API path appended to the base GraphQL endpoint
(e.g. \code{"dar/v2"}, \code{"bbr/v2"},
\code{"mat/v2"})}

\item{query_var_type}{GraphQL scalar type for the time variable
(e.g. \code{"DafDateTime"})}

\item{filter_type}{GraphQL filter input type
(e.g. \code{"DAR_AdresseFilterInput"})}

\item{gql_var}{Name of the GraphQL time variable
(\code{"virkningstid"} or \code{"registreringstid"})}

\item{resource_name}{Name of the GraphQL resource field
(e.g. \code{"DAR_Adresse"})}

\item{where_builder}{A function \code{function(batch_ids)} that returns the
\code{where} list for the GraphQL variables}

\item{nodes}{Character vector of node field names to return}

\item{node_names}{Named character vector mapping API field names to
output column names, or \code{NULL} to skip renaming}

\item{batch_size}{Maximum number of IDs per request}

\item{id_prep}{Optional function for ID pre-processing. Defaults to
\code{function(x) \{ x <- unique(x); x[!is.na(x)] \}}}
}
\value{
A data frame of all nodes across all batches and pages
}
\description{
Encapsulates the full pipeline: ID preparation, input validation,
query building, OAuth client creation, batched fetching with cursor-based
pagination, and column renaming.
}
\keyword{internal}
